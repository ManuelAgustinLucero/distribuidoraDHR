{% extends 'base.html.twig' %}

{% block body %}
    <h1>Export</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ export.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ export.file }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_export_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_export_edit', { 'id': export.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
